
notice: "#"表示已添加至程序中

1. 添加选项：sub_file_name中是否需要timestamp
            因加入timestamp无法验证中断文件夹，继而无法文件夹内是否完整下载
#2. 用面向对象方式重写格式化“文档保存”函数
#3. 用面向对象方式重写格式化“requests”函数
#4. 用面向对象方式重写格式化“selenium”函数
#5.  添加选项：是否创建多个子目录，若为否，则子目录不会生成时间戳，所有文件均存放在子目录中。修改file_making部分
#6.  添加断点检查机制：download_page 中的单页和多页中添加while single_folder:，若不创建多个子目录，则single_folder=True，存储文件前先检查文件是否已经存在，实现断点续传
    若创建多个子目录single_folder=False，因存在时间戳，不检查文件是否已经存在
#7.  添加保存文件的格式选项，不一定.html。可以任何type